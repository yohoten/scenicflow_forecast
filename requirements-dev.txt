# 开发 / 测试依赖（-r 引入运行时依赖）
-r requirements.txt

# 测试
pytest>=7.4
pytest-cov>=4.1

# 如需锁定版本以保证可复现，可执行（需安装 pip-tools）：
#   pip install pip-tools
#   pip-compile requirements-dev.txt -o requirements-dev.lock
